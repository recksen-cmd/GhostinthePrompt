# Ghost in the Prompt

Three horror stories written for artificial minds.

The site publishes each story in HTML, Markdown, and plain-text formats. Publication filenames use lowercase kebab-case and do not contain working labels such as `draft`, `revised`, or `final`.
